# Handoff — readme-release-ia

## Verified E2 seams

- Optional Engram notebook uses an injected CLI adapter only: bounded project-scoped retrieval at enabled session and mapped `map`, `design`, `apply`, and `verify` preparation seams; structured upsert only after a clean artifact gate or archive-first close.
- Fake transport/process coverage verifies pinned argument arrays, `shell: false`, 1,500 ms cancellation, output caps, normalized unavailable/failed outcomes, generated topics, local dedup, and safe receipts.
- OpenSpec remains the canonical complete SDD record. Empty, unavailable, failed search, failed save, and receipt-sidecar failure do not block routing, artifact gates, archive, or close.

## Focused evidence

- `bun test tests/engram-memory-contract.test.ts tests/engram-memory-lifecycle.test.ts` — fake process and lifecycle matrix.
- `bun test tests/sdd-preflight-tdd-gate.test.ts tests/sdd-close.test.ts tests/sdd-router.test.ts` — preflight, close/archive, and routing continuity.
- `bun test tests/sdd-flow-contract.test.ts tests/review-workload-guard.test.ts` — E0/E1/E2 wording, including preflight, status, and doctor.

## Documentation truthfulness gate

README claims may describe only the verified optional-notebook behavior above after these focused fake tests pass. Do not claim real-memory retrieval or persistence.

- E0: configured/diagnosable CLI or MCP state only.
- E1: prompt advice/tool availability only.
- E2: only a named adapter invocation with its truthful receipt.

Non-claims: no MCP client, no direct database access, no mandatory memory, and no full artifact storage in Engram. No real Engram binary, private notebook, or `~/.engram-pi` data was read or written as evidence.
