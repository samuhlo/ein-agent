strict_tdd: true
context: |
  ein-installer is a Node.js/TypeScript ESM project.
  Detected markers: installer/package.json, installer/tsconfig.json, installer/bun package manager, GitHub Actions.
  Package managers: bun.
  Additional evidence: Test-like files detected (51); examples: e2e/docker-test.sh, tests/adhoc-apply-handoff.test.ts, tests/adhoc-hygiene.test.ts, tests/codegraph.test.ts, tests/deploy-clean-managed.test.ts.
  No reliable test runner was detected; verify testing manually before enabling strict TDD.
  Unit tests: none.
  Integration tests: none.
  E2E tests: none.
rules:
  design:
    require_problem_statement: true
    require_acceptance_criteria: true
  apply:
    test_command: ""
  verify:
    test_command: ""
testing:
  detected: "2026-07-13"
  runner:
    command: ""
    framework: ""
  layers:
    unit: ""
    integration: ""
    e2e: ""
  commands:
    unit:
      []
    integration:
      []
    e2e:
      []
  coverage:
    command: ""
    commands:
      []
quality:
  lint: ""
  lint_commands:
    []
  typecheck: "cd installer && bun run typecheck"
  typecheck_commands:
    - scope: "installer"
      command: "cd installer && bun run typecheck"
      framework: "type checker"
  format: ""
  format_commands:
    []
