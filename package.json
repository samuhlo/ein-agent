{
  "name": "ein-agent",
  "private": true,
  "type": "module",
  "description": "Harness de coding-agent sobre Pi Coding Agent y Claude Code.",
  "dependencies": {
    "@opentui/core": "0.5.1",
    "@opentui/solid": "0.5.1",
    "solid-js": "1.9.12"
  },
  "scripts": {
    "setup": "bun install && cd installer && bun install",
    "test": "bun test",
    "clean:workspace": "bun run tooling/clean-workspace.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.3",
    "@earendil-works/pi-tui": "0.84.3",
    "@types/bun": "latest",
    "typescript": "5.9.3"
  }
}
